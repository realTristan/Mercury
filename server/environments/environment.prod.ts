/*
 * If you don't want to use production mode, create a file
 * called environments.ts in this same directory.
*/
export const environment = {
    production: true,
    mongo_password: "YOUR MONGODB PASSWORD",
    port: 9000
};